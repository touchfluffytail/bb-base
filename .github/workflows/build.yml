name: CI

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Prepare
      run: |
        Expand-Archive bbros.zip
        cp bbros/bin/* .
        rm -Recurse bbros/ 
        rm bbros.zip
    - name: Compile
      run: masscompile.bat